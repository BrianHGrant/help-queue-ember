import Ember from 'ember';
import moment from 'moment';

export default Ember.Component.extend({

    // var elapsedTime: setInterval(ticket.timeAgo, 1000);
    // setInterval(function(){
    //   console.log(ticket.timeOpen);
    //   return ticket.timeOpen;
    //   // return moment(Date.now() - model.get('timeOpen'));
    // }, 1000, ticket)

});
